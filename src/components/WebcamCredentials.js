import React from 'react';
import Button from 'react-bootstrap/Button';

function WebcamCredentials(){
    return(
        <div className = "webcam">
            <Button variant="primary">Capture Image</Button>
        </div>
    );
}

export default WebcamCredentials;